<%= simple_form_for(resource, as: resource_name, url: session_path(resource_name), html: { class: "form-horizontal" }) do |f| %>
  <div class="form-group">
    <%= f.input :email,
                required: false,
                autofocus: true,
                input_html: { autocomplete: "email", class: "form-control" },
                label_html: { class: "col-form-label" } %>
  </div>
  <div class="form-group">
    <%= f.input :password,
                required: false,
                input_html: { autocomplete: "current-password", class: "form-control" },
                label_html: { class: "col-form-label" } %>
  </div>
  <div class="form-group form-check">
    <%= f.input :remember_me, as: :boolean, input_html: { class: "form-check-input" }, label_html: { class: "form-check-label" } if devise_mapping.rememberable? %>
  </div>
  <div class="form-group">
    <%= f.button :submit, "Log in", class: "btn btn-primary w-100 rounded-button" %>
  </div>
<% end %>
<hr>
<div class="text-center">
  <%= link_to "Forgot your password?", new_password_path(resource_name), class: "btn btn-link" %>
</div>
